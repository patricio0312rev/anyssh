extension UIIdentifier.Terminal {
    public enum Links {
        public static let open = "terminal.link.open"
        public static let copyLink = "terminal.link.copyLink"
        public static let copy = "terminal.link.copy"
    }
}
